try:
    # Run the "risky" code that might trigger an error.
except SpecificError:
    # Execute this block only if a specific error occurs.
else:
    # Execute this block only if the try block runs perfectly without errors.
finally:
    # Always execute this block, regardless of what happened above.

class bank:
    def __init__(self, name: str, balance: int):
        self.name = name
        self.balance = balance
    def deposit(self, amount: int, division: int) -> int:
        self.balance += amount
        availablebalance = 0
        try:
           availablebalance = self.balance / division
        except Exception as e:
            print(e.args)
        print (availablebalance)
    def withdraw(self,amount: int) -> int:
        self.balance -= amount
        print (f"Available balance after withdrawal: {self.balance}")
        if self.balance < 0:
            raise InsufficientFund("Your balance is low.")

class InsufficientFund(Exception):
    def __init__(self,message):
        #super(InsufficientFund,self).__init__(message)
          super().__init__(message)

# DRIVER CODE
sbi = bank("SBI",1000)
try:
    sbi.deposit(1000,1)
    sbi.withdraw(5000)
except InsufficientFund as e:
   print (e.args)
else:
    print ("No Mapping Exception")
finally:
    print ("Bank is closed")




try:
    digit = 10 / 0
    print(digit)
except Exception as e:
    print(f"An unexpected problem occurred - {e}")

# args is a tuple attribute in which Python stores all arguments passed
# to an exception instance.
try:
    digit = 10 / 0
    print(digit)
except Exception as e:
    print(f"An unexpected problem occurred - {e.args}")

try:
    digit = 10 / 0
    print(digit)
except ZeroDivisionError as z:
    print(f"An unexpected problem occurred - {z}")

print("\n\n")
try:
    list1 = [1,2,3,4]
    for i in range(len(list1)):
        print(i,'-',list1[9])
except Exception as e:
    print(f"An unexpected problem occured - {e}")

print("\n\n")
try:
    list1 = [1,2,3,4]
    for i in range(len(list1)):
        print(i,'-',list1[9])
except IndexError as e:
    print(f"An unexpected problem occured - {e}")

# list1 = [1,2,3,4]
#
# for i in range(len(list1)):
#     print(i,'-',list1[i])








